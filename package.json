{
	"name": "chneau.github.io",
	"scripts": {
		"start": "rsbuild dev",
		"build": "rsbuild build",
		"deploy": "bun run build && gh-pages --dist dist; rm -rf dist",
		"upgrade": "bun update --latest",
		"check": "deno fmt --use-tabs --quiet; oxlint --fix-dangerously --quiet; timeout 3s biome check --write --unsafe .",
		"lint": "tsc --noEmit",
		"all": "bun run upgrade; bun run check; bun run lint"
	},
	"dependencies": {
		"antd": "^6.0.0",
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"styled-components": "^6.1.19"
	},
	"devDependencies": {
		"@rsbuild/core": "^1.6.9",
		"@rsbuild/plugin-react": "^1.4.2",
		"@types/react": "^19.2.7",
		"@types/react-dom": "^19.2.3",
		"gh-pages": "^6.3.0",
		"typescript": "^5.9.3"
	}
}
